(() => {
  const feature: NoAlgorithmsFeature = {
    id: "instagram.search",
    site: "instagram",
    cssPath: "sites/instagram/search/style.css"
  };

  registerNoAlgorithmsFeature(feature);
})();
